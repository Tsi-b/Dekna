// Shared spinner configuration to avoid duplication across UI surfaces.

// Shared CLI-style spinner frames (braille spinner).
// Smooth and commonly used in many modern CLIs.
export const SPINNER_FRAMES = ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"];

// Spinner cadence (ms). Keep consistent across tab title and inline UI.
export const SPINNER_INTERVAL_MS = 90;
